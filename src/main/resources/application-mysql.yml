spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_transfer_application
    username: mticket
    password: mticket
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        storage_engine: innodb
        format_sql: true